#include<bits/stdc++.h>
using namespace std;
typedef long long ll;


int main() {
    int T, n; 
    cin >> T;
    while(T--) {
        scanf("%d", &n);
        for(int  i = 1; i<= n; ++i) scanf("%d", &a[i]);
    }
}