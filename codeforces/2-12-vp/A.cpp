#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

const int maxn = 333333;
int main() {
    int T, n;
    cin >> T;
    while(T--) {
        scanf("%d", &n);
        for(int i = 1; i <= n; ++i) {
            printf("1 ");
        }
        printf("\n");
    }
}