#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int maxn = 233333;
char s[maxn];
bool a[maxn];
ll pre[maxn], dp[maxn];

int main() {
	int n;
	cin >> n;
	scanf("%s", s);
	for(int i = 1; i <= n; ++i) {
		a[i] = s[i - 1] == '1';
	}
	pre[0] = 0;
	for(int i = 1; i <= n; ++i) {
		for()
	}
	
}
