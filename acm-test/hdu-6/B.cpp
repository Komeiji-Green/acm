#include<bits/stdc++.h>
using namespace std;
typedef long long ll;

const int maxn = 23333;
char s[maxn];


int main() {
    int T, n;
    ll b, x;
    cin >> T;
    while(T--) {
        scanf("%s", s);
        n = strlen(s);
        for(int i = 0; i < n; ++i) {

        }
    }
}